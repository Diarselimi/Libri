<nav class="navbar navbar-default navbar-static-top">
    <div class="container">
        <div class="navbar-header">
            <a class="navbar-brand" href="#">
                <img alt="Brand" src="{{ asset('assets/images/book.svg') }}" width="25px">
            </a>
        </div>
        <ul class="nav navbar-nav">
            <li class="{{ 'homepage'|isNavActive(app.request.get('_route')) }}"><a href="{{ path('homepage') }}">{{ 'Home'|trans }}</a></li>
            <li class="{{ 'my_shelfed_books'|isNavActive(app.request.get('_route')) }}"><a href="{{ path('my_shelfed_books') }}" >{{ 'My books'|trans }}</a></li>
            <li class="{{ 'recommend_index'|isNavActive(app.request.get('_route')) }}"><a href="{{ path('recommend_index') }}" >{{ 'Recommended'|trans }}</a></li>
            <li class="{{ 'add_new_book'|isNavActive(app.request.get('_route')) }}"><a href="{{ path('add_new_book') }}" >{{ 'New book'|trans }}</a></li>
        </ul>
        <form class="navbar-form navbar-left" method="get" action="{{ path('search_for_books') }}">
            <div class="input-group">
                <input type="text" name="search" class="form-control" placeholder="Search for...">
                <span class="input-group-btn">
                    <button class="btn btn-default" type="button">{{ 'Go'|trans }}</button>
                </span>
            </div><!-- /input-group -->
        </form>
        {% if is_granted('ROLE_USER') %}
            <ul class="nav navbar-nav navbar-right">
                <li class="dropdown" class="{{ 'my_profile'|isNavActive(app.request.get('_route')) }}">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true"
                       aria-expanded="false"><img src="{{ asset('uploads/avatar/'~app.user.avatar) }}" height="20px" width="20px" alt="..." class="img-circle"> <span class="caret"></span></a>
                    <ul class="dropdown-menu">
                        <li><a href="{{ path('my_profile') }}">{{ 'Profile'|trans }}</a></li>
                        <li><a href="{{ path('logout') }}">{{ 'Logout'|trans }}</a></li>
                    </ul>
                </li>
            </ul>
        {% else %}
            <a href="{{ path('login') }}" class="btn btn-default navbar-btn navbar-right">{{ 'Login'|trans }}</a>
            <a href="{{ path('register_user') }}"
               class="btn btn-default navbar-btn navbar-right">{{ 'Register'|trans }}</a>
        {% endif %}
    </div>
</nav>